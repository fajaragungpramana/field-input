package com.github.fajaragungpramana.field

/**
 * This enum to check and handle drawable position in event listener
 *
 * @property START = its mean, you want action listener in drawable left position of field.
 * @property END = its mean, you wan action listener in drawable right position of field.
 */
enum class DrawablePosition {
    START,
    END
}